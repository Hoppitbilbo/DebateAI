/**
 * @file A container that maintains a specific aspect ratio.
 * @remarks This component is a wrapper around the `AspectRatio` component from `radix-ui/react-aspect-ratio`.
 * It is part of the `shadcn/ui` collection and is useful for embedding content like images or videos
 * at a consistent size.
 * @see https://www.radix-ui.com/primitives/docs/components/aspect-ratio
 * @see https://ui.shadcn.com/docs/components/aspect-ratio
 */

import * as AspectRatioPrimitive from "@radix-ui/react-aspect-ratio"

const AspectRatio = AspectRatioPrimitive.Root

export { AspectRatio }
