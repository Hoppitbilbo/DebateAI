/**
 * @file Re-exports the `WikiInterviewChat` component.
 * @remarks This file is used for backward compatibility to maintain the component's import path.
 * The actual implementation is located in `src/components/wiki-interview/WikiInterviewChat.tsx`.
 */

import WikiInterviewChatComponent from "./wiki-interview/WikiInterviewChat";

// Re-export WikiInterviewChat for backward compatibility
export default WikiInterviewChatComponent;
